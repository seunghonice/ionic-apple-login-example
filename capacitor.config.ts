import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hongsyong.ionicexample',
  appName: 'ExampleIonicApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
